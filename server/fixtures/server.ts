export default class Server {
  handler = class {
    static start() {
      console.log(
        "This is an internal implentation for testing, either you are a tester or you have not defined your own implementation of the Server Interface."
      );
    }
  };
}
